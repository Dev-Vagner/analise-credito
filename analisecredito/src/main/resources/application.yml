spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    listener:
      simple:
        retry:
          enabled: true
          max-attempts: 3

rabbitmq:
  proposta-pendente:
    queue: "proposta-pendente.ms-analise-credito"
  proposta-concluida:
    exchange: "proposta-concluida.ex"